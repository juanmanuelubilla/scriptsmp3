Requerimientos:

sudo apt update
sudo apt install python3-venv python3-pip python-is-python3 python3-tk -y
sudo apt install libmariadb-dev libmariadb-dev-compat build-essential pkg-config
python3 -m venv venv --upgrade-deps
python -m pip install --upgrade pip
pip install pymysql PyQt6 textual tabulate simpleaudio mariadb Pillow mercadopago requests matplotlib pandas




Ejecutar:

#PARA CREARLO
python3 -m venv venv

#PARA ACTIVARLO
source venv/bin/activate

cd facturacion

python main.py